# MapConductor for HERE

HERE Maps provider for MapConductor.

This module currently contains the minimal `HereMapView` / `HereViewState` provider shell: SDK initialization hook, HERE scene loading, map design switching, camera movement, camera callbacks, and map tap/long-press callbacks.
